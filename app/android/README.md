# ♟️ Chess Multiplayer - Android App

Android приложение на базе WebView с полным функционалом веб-версии.

## 🚀 Быстрый старт

### Требования

- Android Studio Hedgehog | 2023.1.1 или новее
- Android SDK 24+ (Android 7.0+)
- Gradle 8.1+
- Kotlin 1.9+

### Установка

#### Шаг 1: Настройка SDK

**Автоматически (Windows):**
```bash
cd app/android
setup.bat
```

**Вручную:**
Создайте `local.properties`:
```properties
sdk.dir=C\:\\Users\\YourName\\AppData\\Local\\Android\\Sdk
```

#### Шаг 2: Скачать библиотеки (ВАЖНО!)

**Автоматически:**
```bash
download-libs.bat
```

**Вручную:**
См. [LIBRARIES_SETUP.md](LIBRARIES_SETUP.md)

Без библиотек приложение не будет работать!

#### Шаг 3: Сборка

1. Откройте проект в Android Studio:
   ```
   File → Open → выберите папку app/android
   ```

2. Дождитесь синхронизации Gradle

3. Подключите устройство или запустите эмулятор

4. Нажмите Run (▶️) или Shift+F10

## 📦 Сборка APK

### Автоматически (рекомендуется):

```bash
cd app/android
build-apk.bat
```

### Вручную:

**Windows (PowerShell):**
```powershell
cd app/android
.\gradlew.bat clean
.\gradlew.bat assembleDebug
```

**Windows (CMD):**
```cmd
cd app\android
gradlew.bat clean
gradlew.bat assembleDebug
```

**Linux/Mac:**
```bash
cd app/android
./gradlew clean
./gradlew assembleDebug
```

APK: `app/build/outputs/apk/debug/app-debug.apk`

### Release версия

```bash
.\gradlew.bat assembleRelease
```

APK: `app/build/outputs/apk/release/app-release.apk`

## 🔑 Подпись APK

1. Создайте keystore:
```bash
keytool -genkey -v -keystore chess-release.keystore -alias chess -keyalg RSA -keysize 2048 -validity 10000
```

2. Создайте `keystore.properties` в корне android/:
```properties
storePassword=your_store_password
keyPassword=your_key_password
keyAlias=chess
storeFile=chess-release.keystore
```

3. Соберите подписанный APK:
```bash
./gradlew assembleRelease
```

## ✨ Возможности

### Полный функционал веб-версии:
- ✅ Мультиплеер онлайн
- ✅ Игра с ботом (3 уровня)
- ✅ Редактор доски
- ✅ Практика матов
- ✅ Шахматные задачи
- ✅ Анализ позиций
- ✅ Чат
- ✅ Система аккаунтов

### Android-специфичное:
- 📱 Адаптивный интерфейс для мобильных
- 🔄 Поддержка поворота экрана
- 💾 Локальное хранилище (WebView localStorage)
- 🌐 Работа офлайн (игра с ботом, задачи)
- 🔋 Оптимизация батареи

### Многоязычность:
- 🇷🇺 Русский
- 🇬🇧 English
- 🇺🇦 Українська

Язык определяется автоматически по настройкам устройства.

## 📁 Структура проекта

```
android/
├── app/
│   ├── src/
│   │   └── main/
│   │       ├── java/com/aogames/chess/
│   │       │   └── MainActivity.kt          # Главная активность
│   │       ├── res/
│   │       │   ├── values/                  # Ресурсы (EN)
│   │       │   ├── values-ru/               # Ресурсы (RU)
│   │       │   ├── values-uk/               # Ресурсы (UK)
│   │       │   ├── layout/                  # Layouts
│   │       │   ├── drawable/                # Иконки
│   │       │   └── mipmap-*/                # Launcher icons
│   │       ├── assets/                      # Веб-файлы
│   │       │   ├── index.html
│   │       │   ├── multiplayer.js
│   │       │   ├── multiplayer.css
│   │       │   ├── i18n.js
│   │       │   └── ...
│   │       └── AndroidManifest.xml
│   ├── build.gradle                         # App build config
│   └── proguard-rules.pro                   # ProGuard rules
├── build.gradle                             # Project build config
├── settings.gradle                          # Project settings
└── gradle.properties                        # Gradle properties
```

## 🔧 Технологии

- **Язык**: Kotlin
- **UI**: WebView (полноэкранный)
- **Минимальная версия**: Android 7.0 (API 24)
- **Целевая версия**: Android 14 (API 34)
- **Архитектура**: Single Activity + WebView

## 📱 Требования к устройству

- Android 7.0 (Nougat) или выше
- 100 MB свободного места
- Интернет (для онлайн игры)

## 🐛 Отладка

### Просмотр логов WebView:

```bash
adb logcat | grep -i "chromium"
```

### Chrome DevTools:

1. Откройте `chrome://inspect` в Chrome
2. Подключите устройство
3. Найдите приложение в списке
4. Нажмите "inspect"

## 📝 Известные проблемы

- WebView может не поддерживать некоторые новые JS API на старых Android
- Для полной функциональности требуется Android 8.0+

## 🚀 Публикация в Google Play

1. Создайте подписанный APK/AAB
2. Создайте аккаунт разработчика Google Play
3. Загрузите APK/AAB
4. Заполните описание и скриншоты
5. Опубликуйте

## 📄 Лицензия

MIT

---

**Версия**: 2.0.0  
**Разработчик**: AOGames Studios
