# 📚 Настройка библиотек для Android

## Проблема

Android WebView не может загружать библиотеки из CDN без интернета.  
Нужно скачать их локально в `app/src/main/assets/`.

## Решение

### Автоматически (рекомендуется):

```bash
cd app/android
download-libs.bat
```

Скрипт скачает все необходимые библиотеки.

### Вручную:

Скачайте следующие файлы в `app/android/app/src/main/assets/`:

#### 1. jQuery (обязательно)
```
https://code.jquery.com/jquery-3.6.0.min.js
→ jquery.min.js
```

#### 2. Chess.js (обязательно)
```
https://cdnjs.cloudflare.com/ajax/libs/chess.js/0.10.3/chess.min.js
→ chess.js
```

#### 3. Chessboard.js CSS (обязательно)
```
https://unpkg.com/@chrisoakman/chessboardjs@1.0.0/dist/chessboard-1.0.0.min.css
→ chessboard.min.css
```

#### 4. Chessboard.js (обязательно)
```
https://unpkg.com/@chrisoakman/chessboardjs@1.0.0/dist/chessboard-1.0.0.min.js
→ chessboard.min.js
```

#### 5. PeerJS (для мультиплеера)
```
https://unpkg.com/peerjs@1.5.2/dist/peerjs.min.js
→ peerjs.min.js
```

#### 6. Stockfish.js (для анализа и бота)
```
https://cdnjs.cloudflare.com/ajax/libs/stockfish.js/10.0.2/stockfish.js
→ stockfish.js
```

## Проверка

После скачивания в `app/src/main/assets/` должны быть файлы:

```
assets/
├── jquery.min.js          ✅
├── chess.js               ✅
├── chessboard.min.css     ✅
├── chessboard.min.js      ✅
├── peerjs.min.js          ✅
├── stockfish.js           ✅
├── index.html
├── multiplayer.js
├── multiplayer.css
├── i18n.js
├── puzzles.js
├── firebase-config.js
└── supabase-multiplayer.js
```

## Сборка

После добавления библиотек:

```bash
gradlew clean
gradlew assembleDebug
```

## Что будет работать

✅ **Без интернета:**
- Игра с ботом
- Шахматные задачи
- Редактор доски
- Практика матов
- Анализ позиций

❌ **Требует интернет:**
- Онлайн мультиплеер
- Firebase аутентификация
- Синхронизация данных

## Альтернатива

Если не хотите скачивать библиотеки, можно:

1. Использовать только с интернетом
2. Добавить проверку в MainActivity:
```kotlin
if (!isNetworkAvailable()) {
    showError("Требуется интернет")
}
```

---

**После добавления библиотек приложение будет полностью функциональным!** 🎉
