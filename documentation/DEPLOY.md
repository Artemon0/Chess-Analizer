# 🚀 Деплой Chess Multiplayer

## 📦 Vercel (рекомендуется)

### Через CLI:

```bash
# Установите Vercel CLI
npm i -g vercel

# Деплой
vercel

# Продакшн деплой
vercel --prod
```

Получите ссылку: `https://chess-xxx.vercel.app`

### Через веб-интерфейс:

1. Зайдите на [vercel.com](https://vercel.com)
2. Нажмите **Import Project**
3. Выберите GitHub репозиторий
4. Нажмите **Deploy**

Vercel автоматически:
- Определит настройки из `vercel.json`
- Задеплоит при каждом push
- Создаст preview для pull requests

---

## 🐙 GitHub Pages

### Настройка:

1. **Загрузите код на GitHub:**
```bash
git add .
git commit -m "Deploy to GitHub Pages"
git push origin main
```

2. **Включите GitHub Pages:**
   - Откройте **Settings** → **Pages**
   - Source: **Deploy from a branch**
   - Branch: **main** → папка **/docs**
   - Нажмите **Save**

3. **Готово!**
   - Сайт будет доступен: `https://username.github.io/repo-name/`
   - Обновляется автоматически при push

### Обновление файлов:

Файлы в папке `docs/` - это копия корневых файлов для GitHub Pages. При изменениях обновите их:

```bash
# Скопируйте изменения в docs/
copy index.html docs/
copy multiplayer.html docs/
copy multiplayer.js docs/
copy multiplayer.css docs/
copy puzzles.js docs/
copy firebase-config.js docs/
copy supabase-multiplayer.js docs/
```

---

## 🖥️ Electron приложение

### Сборка для Windows:

```bash
cd app
npm install
npm run build
```

Готовое приложение в `app/dist/`

### Сборка для других платформ:

Отредактируйте `app/package.json`:

```json
"build": {
  "win": { ... },
  "mac": { ... },
  "linux": { ... }
}
```

Затем:
```bash
npm run build
```

---

## 🔧 Настройка Firebase (опционально)

Для синхронизации аккаунтов между устройствами:

1. Создайте проект на [Firebase Console](https://console.firebase.google.com/)
2. Включите **Realtime Database**
3. Скопируйте конфигурацию в `firebase-config.js`:

```javascript
const firebaseConfig = {
    apiKey: "ВАШ_API_KEY",
    authDomain: "ваш-проект.firebaseapp.com",
    databaseURL: "https://ваш-проект-default-rtdb.europe-west1.firebasedatabase.app",
    projectId: "ваш-проект",
    storageBucket: "ваш-проект.appspot.com",
    messagingSenderId: "123456789",
    appId: "1:123456789:web:abc123"
};
```

4. Настройте правила безопасности в Firebase Console:

```json
{
  "rules": {
    "users": {
      "$uid": {
        ".read": true,
        ".write": "auth != null"
      }
    }
  }
}
```

Без Firebase данные сохраняются локально в localStorage.

---

## ✅ Проверка деплоя

После деплоя проверьте:

- ✅ Открывается главная страница
- ✅ Работает создание игры
- ✅ Работает игра с ботом
- ✅ Работают шахматные задачи
- ✅ Работает анализ
- ✅ Работает чат
- ✅ Работает система аккаунтов
- ✅ Работает смена темы и стилей

---

## 🌐 Как работает мультиплеер

- **PeerJS** - игроки подключаются напрямую (peer-to-peer)
- **Без сервера** - работает на статическом хостинге
- **Бесплатно** - Vercel и GitHub Pages бесплатны
- **Быстро** - CDN по всему миру

### Процесс игры:

1. Игрок 1 нажимает "Создать игру"
2. Получает уникальную ссылку
3. Отправляет ссылку Игроку 2
4. Игрок 2 открывает ссылку
5. Соединение устанавливается напрямую
6. Играют!

---

**Готово! Приятной игры! ♟️**
