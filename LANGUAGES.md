# 🌐 Многоязычность

Приложение поддерживает несколько языков с автоматическим сохранением выбора.

## Поддерживаемые языки

- 🇷🇺 Русский (по умолчанию)
- 🇬🇧 English

## Переключение языка

Используйте кнопки с флагами в правом верхнем углу интерфейса.

Выбранный язык сохраняется в localStorage и применяется автоматически при следующем запуске.

## Добавление нового языка

### 1. Добавьте переводы в `i18n.js`

```javascript
const translations = {
    ru: { ... },
    en: { ... },
    // Добавьте новый язык
    es: {
        'title': '♟️ Chess Multiplayer',
        'subtitle': 'Juega en línea + análisis en tiempo real',
        'createGame': '🎮 Crear juego',
        // ... остальные переводы
    }
};
```

### 2. Добавьте кнопку в HTML

В `index.html` добавьте кнопку переключения:

```html
<div class="language-selector">
    <button class="lang-btn active" data-lang="ru">🇷🇺</button>
    <button class="lang-btn" data-lang="en">🇬🇧</button>
    <button class="lang-btn" data-lang="es">🇪🇸</button> <!-- Новая кнопка -->
</div>
```

### 3. Синхронизируйте файлы

```bash
# Скопируйте изменения во все папки
copy i18n.js docs\i18n.js
copy i18n.js app\renderer\i18n.js
copy index.html docs\index.html
copy index.html app\renderer\index.html
```

## Структура переводов

Все переводы находятся в файле `i18n.js` в объекте `translations`.

### Категории переводов:

- **Заголовки** - title, subtitle
- **Кнопки управления** - createGame, joinGame, playBot, etc.
- **Контроль времени** - bullet, blitz, rapid, classical
- **Статусы игры** - gameCreated, yourTurn, checkmate, etc.
- **Игроки** - white, black, you, opponent
- **Анализ** - analysisTitle, bestMove, brilliant, etc.
- **Чат** - chat, quickChat, goodGame, etc.
- **Аккаунты** - login, register, username, password, etc.
- **Сообщения** - welcome, loggedOut, etc.
- **Настройки** - settings, pieceStyle, boardColor, etc.

## Использование в коде

### Получение перевода

```javascript
const text = t('createGame'); // Вернет перевод для текущего языка
```

### Смена языка

```javascript
setLanguage('en'); // Переключит на английский
```

### HTML атрибуты

Для автоматического перевода используйте атрибут `data-i18n`:

```html
<button data-i18n="createGame">🎮 Создать игру</button>
<input data-i18n="username" placeholder="Имя пользователя" />
<h3 data-i18n="analysisTitle">📊 Анализ позиции</h3>
```

Для title атрибутов используйте `data-i18n-title`:

```html
<button data-i18n-title="darkTheme">🌙</button>
```

## Динамический контент

Для динамически создаваемого контента используйте функцию `t()`:

```javascript
$('#gameStatus').text(t('yourTurn'));
addChatMessage('system', t('opponentJoined'));
alert(t('confirmResign'));
```

## Тестирование

1. Откройте приложение
2. Переключите язык с помощью кнопок
3. Проверьте все разделы:
   - Заголовки и кнопки
   - Модальные окна
   - Сообщения в чате
   - Статусы игры
   - Настройки

## Примечания

- Эмодзи и иконки сохраняются во всех языках
- Числа и таймеры не переводятся
- Имена пользователей остаются без изменений
- Выбранный язык сохраняется в `localStorage`

---

**Нужна помощь с переводом?** Создайте issue на GitHub!
